exit
@student
@student.active ? @student.active = false : @student.active = true
@student.active
exit
student_path(@student)
link_to(@student, student_path(@student)) 
@student.active ? false : true
@student = Student.find(params[:id])
exit
!!!
link_to(student, student_path(student))
link_to(student, students_path(student))
students_path(Student.all.first)
students_path
students_path(student)
students_path
student
link_to(student, students_path(student))
exit
students_path(student)
student_path(student)
student_path
student
students_path(student)
student_path(student)
exit
students_path
exit
students_path
student_path
@student_path
@student
@path
@student = Student.find(params[:id])
@student = Student.find(id: params[:id])
params[:id]
params
@student = Student.find(id: params[:id])
Student.all
